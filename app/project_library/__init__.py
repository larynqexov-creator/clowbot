"""Project Library (Capture) domain.

Provides API + processing pipeline to capture materials into a project-scoped library,
index them for search (best-effort RAG), and keep a live project mindmap updated.
"""
